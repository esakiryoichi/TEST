# タスク期間カレンダー

タスク名・開始日・終了日を入力して、月ごとのカレンダーにタスク期間を横棒で表示するシングルページです。デフォルトでサンプルデータが3か月分描画され、フォームから追加するとカレンダーが自動で拡張されます。

## 使い方

1. `index.html` をブラウザで開きます。
2. 「タスク名」「開始日」「終了日」を入力して **カレンダーに追加** を押します。
3. 期間が重なる場合は別レーンに自動で配置され、3か月以上の期間でも月が追加されます。

PCでの閲覧推奨ですが、横スクロールで月を移動できるためスマートフォンでも確認できます。
